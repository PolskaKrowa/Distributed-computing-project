# tests/mocks/CMakeLists.txt
# Mock implementations and test fixtures

# Placeholder for mock/fixture sources
# Shared test utilities and mock objects for testing

message(STATUS "Test mocks and fixtures: (sources pending)")
